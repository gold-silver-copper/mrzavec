# Rogue 5.4.5 feature parity audit

This document is the requirement ledger. An entry may be marked verified only
after comparison with the named C implementation and behavior-focused tests.

| Feature area | C authority | Rust implementation/tests | Status |
| --- | --- | --- | --- |
| 80×24 glyph presentation | `io.c`, `rogue.h` | `main.rs`; exact 1/22/1 message-map-status row partition, reference status fields/hunger format, identification-safe equipment views, 1,920-cell buffer, overlay and `--More--` clear-screen pagination tests | verified |
| seeded level layout and connectivity | `new_level.c`, `rooms.c`, `passages.c` | `generation.rs`, `game.rs`; original rooms→passages→objects→traps→stairs RNG/placement order, original 8-line room bands, normal/gone/dark/maze rooms, weighted maze starts, randomized wall endpoints/turn points, persistent hidden corridor intersections, connected-component passage numbering across doors and mazes, oriented secret doors, room-weighted floor selection, documented adjacent-wall connectivity fix, no maze traps, item/trap/stair exclusion, maze stairs, connectivity, placement, and stairs tests | verified |
| movement and collision | `move.c`, `command.c`, `rooms.c` (`find_floor`) | `game.rs`, `command.rs`, `main.rs`; free blocked/confused moves, strict diagonal corners, distinct momentum-style uppercase running versus nearby-danger/intersection-sensitive control running, pickup and invisible-monster information rules, move-without-pickup object reporting, reference pre-command `look` wake timing across ordinary, repeated, counted, sleep, quit, and save commands; whole-room doorway waking; reference trap hit formulas/messages/RNG, revealed-glyph gating and hallucinated trap-identification state without hidden-trap information leaks, concealed-trap processing before the flytrap hold check, underlying-map `fallpos` arrow placement including monster overlap, rejected-room/index/x/y `find_floor` RNG shared by scroll, trap, ring, and wizard player teleportation, sleep-command timing, bear/flytrap holds, immediate blocked-corner and doorway-aware passgo, and haste-phase tests | verified |
| visibility and memory | `move.c` (`look`, `light`), `misc.c` (`trip_ch`), `daemons.c` (`visuals`), `potions.c` (`turn_see`, `seen_stairs`), `scrolls.c` (`S_MAP`) | `game.rs`, `main.rs`; squared lamp radius, strict blocked-corner memory, rendered and remembered revealed traps, room/darkness memory, true-type detection, depth-aware hallucinated objects/Xerocs/unknown stairs with per-entity gameplay-RNG consumption and frame-stable persisted glyph snapshots, `jump` suppression of intermediate automatic-run `visuals` updates, blind monster-detection handling, persisted stair knowledge, magic-detection map and original carried-magic guard quirk, remote-monster and magic-map information-leak tests | verified |
| objects and generation | `extern.c`, `things.c`, `new_level.c`, `init.c`, `weapons.c`, `sticks.c`, item files | `item.rs`, `game.rs`, `main.rs`; field-by-field category and subtype probability tables; exact armor/weapon/ring curse and enchantment thresholds, missile quantities/groups, and wand/staff charge and melee-damage initialization; startup arrow/scroll/color/stone/material RNG ordering, food drought, treasure-room and Amulet-depth rules, reference `give_pack` call sites, trap-safe placement, appearance, invalid/wielded-stack potion and scroll semantics, all fourteen potion effect/identification branches including poison/raise-level message ordering and the level-21 experience-table sentinel, all eighteen scroll branches including silent armor enchantment without armor, row-major create-monster reservoir RNG, independent monster-detection fuses, potion cleanup, hallucinated scroll colors, held-monster run-state clearing, passage-aware teleport-scroll identification, exact hold/identify/enchantment forms, food/magic detection overlays, complete stick subtype dispatch including rendered-glyph targeting that passes undetected invisible monsters, cancellation preserving `ISHUH`, magic-missile target naming/message ordering and unchanged state on magic saves, shared `runto` wake/hold/destination transitions, rejected-room `find_floor` RNG, hero redirection, polymorph run-state reset, and aggravation; duplicate-ring daemon semantics, poison cleanup, adjacent-create-scroll tests, signed wizard gold, arbitrary master-created glyph objects, and full-pack wizard equipment state | verified |
| inventory and equipment | `pack.c`, `things.c`, `armor.c`, `weapons.c`, `rings.c`, `misc.c` | `player.rs`, `game.rs`, `main.rs`; C-style inventory articles, counts, plurals, appearances, bonuses and charges used consistently by full, slow, picky, winner, and wizard ground-object views; reference empty-handed forms, single-item `I`, terse/verbose picky prompts, invalid-letter termination, and no synthetic glyph/`xN` columns; `get_item`-style `*` retry and `unctrl` invalid-key feedback; exact restricted-identification retry categories, identified-item descriptions, empty post-scroll packs, and repeated identify-scroll continuation; post-split drop descriptions; shared `dropcheck` release behavior for wielded/worn/ring items including the original simultaneous wielded/worn armor edge, post-dropcheck throw rejection, and one-turn removal; reference turn consumption for invalid, inedible, and cursed attempts; noncursed equipped-item throwing; pre-prompt cursed-weapon rejection; exact ring-hand prompts, invalid-key retries, and cancellation timing; reference pickup/drop/full-pack/move-on and equipment messages including empty, levitating, gold, revealed-trap rejection, and already-armored forms; nonblocking current-equipment/status messages; stable/reused pack letters, category ordering, 26-unit capacity accounting, counted-consumable versus grouped-missile full-pack merging, type/subtype-only multiplied-item stacking, stack theft, two-turn normal armor changes, wielded-food clearing, starvation-deficit reset on eating, terse inedible handling, all passive ring modifiers/food costs, left-then-right automatic searching and independent teleportation rolls including duplicate rings, original shared see-invisible behavior, nonweapon damage, and terse-message tests | verified |
| player stats and progression | `extern.c` (`INIT_STATS`, `e_levels`), `misc.c` (`check_level`, `chg_str`), `fight.c` (wraith/vampire drain), `io.c` (`status`) | `player.rs`, `game.rs`, `main.rs`; exact initial stats and all 20 experience thresholds/level 21; boundary tests at every threshold; final-level-only HP gain and welcome message for multi-level jumps; experience-loss level correction without HP loss; wraith threshold indexing and max-HP drain; 3–31 strength bounds with add-strength bonuses excluded from recorded maximum; armor, HP-width, hunger, and experience status forms | verified |
| monsters and AI | `extern.c`, `monsters.c`, `chase.c`, `fight.c` | `monster.rs`, `game.rs`; field-by-field match for all 26 template names, carry rates, flags, XP, levels, armor, and damage strings; exact normal selection order and wandering exclusions; depth/experience/level-30 haste scaling, depth-aware Xeroc disguises, slow/haste/flying scheduling, fallible raw random moves for bats/phantoms/confusion, strict diagonal attack/wake corners, row-major pre-action nearby wakes and whole-room doorway wakes without premature monster actions, passage-first `roomin` area identity, same-area dragon breath through the shared six-cell reflecting bolt engine, visibility-aware item diversion, nonworsening chase selection, player-room exclusion for wanderers, reference-inert `ISREGEN`, aggravation, carried-pack, greed/gold, Medusa gaze, confusion, exact nymph eligible-item reservoir RNG, single-stack theft, and named-theft message tests | verified |
| combat and special attacks | `fight.c`, `weapons.c`, `sticks.c` | `combat.rs`, `game.rs`; dice/hit including successful zero-damage melee/confusion, exact RNG-selected verbose and target-suppressing terse hit/miss tables, visibility-safe and hallucinated monster naming, launcher-dependent arrow damage and bow bonuses, thrown-Xeroc reveal/continued attack, successful-projectile consumption, miss/wall `fallpos`, randomized carried-pack drops including monster overlap, silent ice misses and terse/verbose freeze text, flytrap hit/miss constriction escalation, damage/message ordering, and independent hold state, reference targeted-stick hold release while preserving global constriction damage, six-cell elemental-bolt reflection, magic saves, dragon immunity, `runto` pursuit, real-Medusa exception, and terse/verbose miss text; wraith/vampire death causes, confusion/hold/cancellation, saving throw, polymorph, and charge tests | verified |
| commands and prompts | `command.c`, `io.c`, `wizard.c` | `command.rs`, `main.rs`; complete normal/master key parsing including legal space, redraw, shell/suspend adaptation, control-run, reference direction-before-item throw/zap prompts, empty-pack and repeat-runout per-command turn rules, counted multi-step prompts, saved item/direction repetition, normal/kamikaze fight-to-death, nonblocking equipment/status queries, pre-prompt wield curse checks, ring-hand cancellation timing, case-preserving manual and post-use potion/scroll call prompts including exact terse/verbose forms and repeated prefilled flow, case-sensitive hidden wizard-password entry and dungeon-number activation text, reference trap-identification messages, raw recall versus `endmsg` display capitalization, queued compound messages with `--More--` before deferred prompts, tab stops, full-help truncation and 23-row pagination, exact version/release/dungeon-number reporting, quit and save-file/overwrite confirmation, plus Bevy tests for glyph identification, randomized filtered discoveries, call defaults, and inventory modes | verified |
| daemons, fuses, hunger, healing | `daemon.c`, `daemons.c` | `effects.rs`, `game.rs`; daemon/fuse ordering including RNG-consuming, `after`-gated hallucination visuals, armor `waste_time`, end-cycle automatic rings, quiet/healing thresholds, dual regeneration, all ring food costs, hunger/faint/starvation boundaries, haste phases, normal/hallucinated expiration messages, and wandering tests | verified |
| depth, Amulet, ascent, victory | `new_level.c`, `move.c`, `rip.c` | `game.rs`; free stair commands, levitation/blocking, new-level hold cleanup, max-depth suppression, repeated deep Amulet placement, wrenching ascent, depth-zero victory, and winner-sale tests | verified |
| death, quit, scores, tombstone | `rip.c`, `common.c`, `scmisc.c`, `scedit.c` | `game.rs`, `score.rs`, `main.rs`; persisted and displayed death causes, C `killname` article rules for monsters, projectiles, signals, starvation, and hypothermia in tombstone and plain death views, death tax, exact quit prompt/cancellation/output, field-by-field winner valuation tables/formulas including randomized ring-stone value and the source's carried-forward `worth` for master-only gold/arbitrary objects, reference score reasons, strict positive-score insertion, no rewrite when a result cannot enter the table, one-best-nonwinner rule, corrupt-table preservation, no-score suppression, lock-contention in-memory display without persistence, full fixed-coordinate grave overlays without clipping long names, and reference identified inventory forms and values in winner appraisal/sale tests | verified |
| save and restored continuation | `save.c`, `state.c` | `save.rs`, `main.rs`; reference filename/overwrite prompts, successful-save exit, safe main-thread signal autosave and `-S` untaxed signal death, read-only permissions, restore-path adoption, reusable/hard-link-tolerant wizard saves, and single-use normal restores; exclusive same-directory temporary creation and atomic replacement; version/dead-state validation; validated-open-file identity, regular-file, symlink, and normal-mode hard-link checks; invalid-file preservation; complete map, actor, equipment, knowledge, timers, message/recall sequence, RNG, options, and repeat state; full-state round-trip/RNG/repeat-state equivalence tests; CLI fail-closed restore | verified |
| options, discovery, help, wizard mode | `main.c`, `options.c`, `misc.c`, `things.c`, `wizard.c` | pure-tested native launcher parsing for `-V`, `-h`, default/custom `-s`, `-d`, ignored `-r`, accepted `-S`, invalid options, empty-first-argument wizard gating, positional restore, and 32-bit C-style `$ROGOSEED`/wizard `$SEED`; all editable boolean, inventory, name, fruit, save/score/lock options; `ROGUEOPTS` reference abbreviations, negations, repeated separators, inventory prefixes, `~` expansion, printable filtering, and 50-character limit; reference clear-screen default plus distinct clear, overwrite, and slow inventory/discovery flows; complete printed/single-key normal-command help with `unctrl` labels, distinct fight modes, and reference unknown-key handling; exact glyph identification table; appearance-prefilled calling, post-use calling, Rogomatic aliases, identified-guess cleanup, guesses in discovery lists, reference terse/verbose retry prompts, RNG-shuffled discovery ordering, category-specific empty text, and one-line discovery recall/clear behavior; identification and charge empty-pack suppression/messages, password gate, master control keys including level-zero ascent, non-mutating true-map display, exact creation subtype ranges/retry prompts, wizard gold and Amulet state, reference blessing/object formation, charge, and power commands | verified |

Every row was rechecked against the named C source and the complete Rust diff in
the final audit. Feature parity is declared with no incomplete checklist entry.
